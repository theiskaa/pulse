import 'package:flutter/widgets.dart';
import 'package:pulse/view/screens/base.dart';

class Home extends StatelessWidget {
  const Home({super.key});

  @override
  Widget build(BuildContext context) {
    return const BaseScreen();
  }
}
